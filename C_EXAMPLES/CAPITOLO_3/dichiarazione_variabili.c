#include<stdio.h>
#include<math.h>
int main(void) {
int a; //intero con segno a 32 bit
float b; // floating point 32 bit
double c; // fp a 64 bit
char d='c'; // un carattere
char d2; // intero con segno a 8 bit (rappresentazione in complemento a 2)
long long int e; // intero con segno a 64 bit (rappresentazione in complemento a 2)
unsigned int ua0; // intero positivo a 32 bit 
unsigned char uc; // intero positivo a 8 bit 
long double lc; // precisione quadruipla 128 bit flotaing point (ma di fatto se ne usano 96)
}
